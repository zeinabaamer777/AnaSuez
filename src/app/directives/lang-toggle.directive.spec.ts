import { LangToggleDirective } from './lang-toggle.directive';

describe('LangToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new LangToggleDirective();
    expect(directive).toBeTruthy();
  });
});
