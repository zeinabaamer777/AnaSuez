import { DateHandlerDirective } from './date-handler.directive';

describe('DateHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new DateHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
